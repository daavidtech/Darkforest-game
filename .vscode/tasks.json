{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Start dev server",
			"type": "shell",
			"command": "bun run dev",
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"runOptions": {
				"runOn": "folderOpen"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": []
		}
	]
}
